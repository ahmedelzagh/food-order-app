import Card from "../Components/UI/Card";

const NotFound = () => {
  return <Card style={{ display: "flex", justifyContent: "center", margin: "auto" }}>Page Not Found</Card>;
};

export default NotFound;
